# Copy to .env and fill in. .env is gitignored and never leaves this machine.
# Prefer an account scoped to what the scripts actually need over a superuser:
# see docs/CREDENTIALS.md.
PB_URL=https://poolean-api.adammirmina.com
PB_EM=
PB_PW=
